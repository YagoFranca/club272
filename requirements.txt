# Dependências do 272 Club (Python 3.10+)
# tkinter e sqlite3 fazem parte da biblioteca padrão — no Windows já vêm com
# o instalador oficial do Python; no Linux instale `python3-tk`.

# Interface
customtkinter>=5.2
pillow>=10.0

# Visão computacional / reconhecimento facial
opencv-python>=4.8
face-recognition>=1.3
face-recognition-models>=0.3
dlib>=19.24
numpy>=1.23

# Relatórios
openpyxl>=3.1          # planilhas .xlsx

# Integração
requests>=2.31
python-dotenv>=1.0
